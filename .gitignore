# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
.shopify
node_modules
*.zip

# Impeccable local/session cache — .impeccable/surfaces (direction contracts)
# is committed, this cache is not.
.impeccable/hook.cache.json
.impeccable/config.local.json